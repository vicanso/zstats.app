name: "Publish"

# Adapted from vicanso/zedis's publish workflow, trimmed to macOS only:
# this app is a macOS menu-bar panel — the other platforms' cfg branches
# have never been run (see CLAUDE.md "Platform reality"), so there is
# nothing to ship for them.
#
# Required repository secrets:
#   MACOS_CERTIFICATE       base64-encoded Developer ID Application .p12
#   MACOS_CERTIFICATE_PWD   password of that .p12
#   MACOS_CERTIFICATE_NAME  signing identity, e.g. "Developer ID Application: … (TEAMID)"
#   AC_USERNAME             Apple ID used for notarization
#   AC_PASSWORD             app-specific password for that Apple ID
#   AC_TEAM_ID              Apple Developer team id
#   GITEE_TOKEN             Gitee personal token (projects scope) for the
#                           release mirror at gitee.com/vicanso/zstats.app

on:
  push:
    tags: ["v[0-9]+.[0-9]+.[0-9]+*"]
  schedule:
    # Daily nightly at 18:00 UTC (02:00 Beijing). Skipped when main has no
    # new commits since the last nightly — see the gate in prepare_vars.
    - cron: "0 18 * * *"
  # Manual nightly (always builds, even without new commits), or — with
  # `mirror_tag` — a mirror-only run that builds nothing and just copies
  # an already-published release to Gitee. That entry point exists
  # because re-running a failed job replays the *old* workflow file, so a
  # fix to the mirror can never reach the release that tripped over it.
  workflow_dispatch:
    inputs:
      mirror_tag:
        description: "Mirror this existing release to Gitee and build nothing (e.g. v0.3.1)"
        required: false
        default: ""


# The default GITHUB_TOKEN is enough for everything this workflow does
# (create/delete releases and tags in this repo); no PAT needed.
permissions:
  contents: write

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  CARGO_TERM_COLOR: always
  RUST_TOOLCHAIN: 1.98.1
  # [package.metadata.bundle] name in Cargo.toml — names the .app.
  APP_NAME: zstats

jobs:
  prepare_vars:
    runs-on: ubuntu-latest
    outputs:
      build_date: ${{ steps.date.outputs.date }}
      should_build: ${{ steps.gate.outputs.should_build }}
    steps:
      - name: Get current date
        id: date
        run: echo "date=$(date +'%Y-%m-%d %H:%M')" >> $GITHUB_OUTPUT
      - uses: actions/checkout@v7
        with:
          # Full history + tags: the draft release's changelog needs the
          # previous v* tag (`git describe` below) to bound its range —
          # a shallow clone cannot see it and the generated notes would
          # silently cover the whole history.
          fetch-depth: 0
      - name: Check tag matches Cargo.toml version
        # Catches a forgotten version bump before an hour of signing and
        # notarization is spent on a release that claims the wrong version.
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          ver="$(sed -n 's/^version = "\([^"]*\)"/\1/p' Cargo.toml | head -1)"
          case "$GITHUB_REF_NAME" in
            "v$ver"*) echo "tag $GITHUB_REF_NAME matches Cargo.toml version $ver" ;;
            *) echo "tag $GITHUB_REF_NAME does not match Cargo.toml version $ver" >&2; exit 1 ;;
          esac
      - name: Skip scheduled run when main has no new commits
        id: gate
        env:
          MIRROR_TAG: ${{ inputs.mirror_tag }}
        run: |
          should_build=true
          # A mirror-only run builds nothing: the release it copies was
          # built, signed and notarized when its tag was pushed.
          if [ -n "$MIRROR_TAG" ]; then
            echo "mirror-only run for $MIRROR_TAG; skipping the build"
            should_build=false
          elif [ "${{ github.event_name }}" = "schedule" ]; then
            # The nightly tag records the commit the previous nightly was
            # built from (re-created below at $GITHUB_SHA on every build).
            nightly_sha=$(gh api "repos/${{ github.repository }}/git/refs/tags/nightly" -q .object.sha 2>/dev/null || echo "")
            if [ "$nightly_sha" = "${{ github.sha }}" ]; then
              echo "main unchanged since last nightly ($nightly_sha); skipping build"
              should_build=false
            fi
          fi
          echo "should_build=$should_build" >> $GITHUB_OUTPUT
      - name: Reset nightly release
        # Sweep by id via the API, not `gh release delete nightly`: that
        # removes only the published release, while GitHub allows any
        # number of *drafts* to share a tag_name, and a job that died
        # mid-publish can leave orphan drafts behind (this fed a zedis
        # nightly failure). Then re-create the release up front, before
        # any build job runs: `--target $GITHUB_SHA` re-points the
        # nightly tag at today's commit (which is what the gate above
        # compares against), and having the release exist first means the
        # build jobs only ever `gh release upload` — no create race.
        if: github.ref == 'refs/heads/main' && steps.gate.outputs.should_build == 'true'
        run: |
          ids="$(
            gh api --paginate "repos/${{ github.repository }}/releases" \
              -q '.[] | select(.tag_name == "nightly") | .id' 2>/dev/null || true
          )"
          for id in $ids; do
            gh api -X DELETE "repos/${{ github.repository }}/releases/$id" || true
          done
          gh api -X DELETE "repos/${{ github.repository }}/git/refs/tags/nightly" 2>/dev/null || true
          gh release create nightly --prerelease --target "$GITHUB_SHA" \
            --title "Development Build (Nightly)" \
            --notes "Rolling build from the latest main branch. Build date: ${{ steps.date.outputs.date }}"
      - name: Install git-cliff
        if: startsWith(github.ref, 'refs/tags/')
        uses: taiki-e/install-action@v2
        with:
          # Pinned: the notes this produces are the release's public
          # face, and an unpinned tool changes what that looks like
          # without a commit to point at.
          tool: git-cliff@2.13.1
      - name: Pre-create draft release for tag
        # Two arch jobs upload in parallel; creating the release here,
        # before either runs, removes the create race. Skip when ANY
        # release for the tag already exists — including a hand-published
        # one (releasing via the GitHub UI is what pushes the tag) and
        # the draft of a re-run; `gh release create` alone would still
        # add a draft next to a published release, and the uploads would
        # land on that draft instead of the published release.
        #
        # Probe via the Releases API (paginated), not `gh release list |
        # grep` — list+grep has missed published releases before and then
        # left empty untagged drafts beside the real release.
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          tag="$GITHUB_REF_NAME"
          ids="$(
            gh api --paginate "repos/${{ github.repository }}/releases" \
              -q ".[] | select(.tag_name == \"${tag}\") | .id" 2>/dev/null || true
          )"
          # Notes come from git-cliff (commit-based, cliff.toml — the
          # same source and format as CHANGELOG.md). Not GitHub's
          # generator: that one lists merged PRs, and this repo pushes
          # to main directly, so it emits nothing but a compare link.
          # --latest = the most recent tag's section; cliff.toml's
          # tag_pattern (v[0-9].*) already keeps the rolling `nightly`
          # tag out of the picture.
          git cliff --latest --strip header > /tmp/notes.md || true
          # Trust nothing: git-cliff exits 0 having produced a release
          # header with no entries whenever a commit_preprocessor
          # silently eats the messages (a `replace_command` whose binary
          # is missing did exactly that to v0.1.2 — CI has no typos
          # binary, a dev machine does, so it passed review and shipped
          # an empty section). A changelog with no list items is a bug
          # in this workflow, not a quiet release, so say so and stop.
          if ! grep -q '^- ' /tmp/notes.md; then
            echo "git-cliff produced no entries for $tag — refusing to publish empty notes" >&2
            echo "--- what it produced ---"; cat /tmp/notes.md
            exit 1
          fi
          if [ -n "$ids" ]; then
            # Publishing through the GitHub UI is what pushes the tag, so
            # this branch IS the normal release path — and that release
            # usually has an empty body. Backfill into an empty body
            # only; hand-written notes are never overwritten.
            body="$(gh release view "$tag" --json body -q .body 2>/dev/null || true)"
            if [ -z "$body" ] && [ -s /tmp/notes.md ]; then
              echo "backfilling git-cliff notes for $tag"
              gh release edit "$tag" --notes-file /tmp/notes.md
            else
              echo "release for $tag already has notes; leaving them"
            fi
          else
            gh release create "$tag" --draft --title "$tag" \
              --notes-file /tmp/notes.md
          fi

  macos:
    needs: prepare_vars
    if: needs.prepare_vars.outputs.should_build == 'true'
    name: macos-${{ matrix.dmg_suffix }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            target: aarch64-apple-darwin
            dmg_suffix: aarch64
          - os: macos-15-intel
            target: x86_64-apple-darwin
            dmg_suffix: x86_64
    steps:
      - name: preflight notary credentials
        # A bad AC_* secret otherwise surfaces only after build, sign and
        # dmg have spent their ten minutes — the same catch-it-early
        # reasoning as the tag/version check in prepare_vars. `history`
        # is the cheapest authenticated notarytool call; a stale
        # app-specific password (revoked wholesale whenever the Apple ID
        # password changes) fails here in seconds instead.
        env:
          AC_USERNAME: ${{ secrets.AC_USERNAME }}
          AC_PASSWORD: ${{ secrets.AC_PASSWORD }}
          AC_TEAM_ID: ${{ secrets.AC_TEAM_ID }}
        run: |
          xcrun notarytool history --apple-id "$AC_USERNAME" \
            --password "$AC_PASSWORD" --team-id "$AC_TEAM_ID" >/dev/null
          echo "notary credentials OK"
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}
          targets: ${{ matrix.target }}
      - name: rust cache
        uses: Swatinem/rust-cache@v2
        with:
          key: macos-build-${{ matrix.target }}
      - name: install tools
        # The cache restore above runs first so `cargo install` no-ops
        # when rust-cache restored the same cargo-bundle version to
        # ~/.cargo/bin.
        #
        # --locked + pinned version: an unlocked install resolves the
        # tool's deps fresh, so a broken transitive release would break
        # the nightly (a since-yanked zune-core release did exactly that
        # to zedis).
        run: |
          cargo install --locked cargo-bundle@0.11.0
          brew install create-dmg
      - name: Import Code-Signing Certificates
        env:
          p12_certificate: ${{ secrets.MACOS_CERTIFICATE }}
          p12_password: ${{ secrets.MACOS_CERTIFICATE_PWD }}
        run: |
          # create temporary keychain
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
          security create-keychain -p "" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "" "$KEYCHAIN_PATH"

          # import certificate. $RUNNER_TEMP, never the checkout: a file
          # left in the workspace makes `git status --porcelain` non-empty,
          # and build.rs reads that as "not this commit" and stamps the
          # About page `<sha>-dirty`. A decoded signing certificate also
          # has no business sitting inside the tree being built.
          CERT_PATH="$RUNNER_TEMP/certificate.p12"
          echo "$p12_certificate" | base64 --decode > "$CERT_PATH"
          security import "$CERT_PATH" -k "$KEYCHAIN_PATH" -P "$p12_password" -T /usr/bin/codesign
          rm -f "$CERT_PATH"

          # prevent system popup asking for password
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "" "$KEYCHAIN_PATH"

          # set as default keychain
          security list-keychains -d user -s "$KEYCHAIN_PATH"
      - name: workspace must be clean before the build
        # build.rs stamps the About page with the HEAD sha, plus `-dirty`
        # whenever `git status --porcelain` is non-empty at compile time —
        # untracked files included. A released, signed, notarized binary
        # that cannot be traced to a commit is a provenance hole, and the
        # way it happened once was a step writing certificate.p12 into the
        # checkout. Fail here, before the ten minutes of signing and
        # notarization, rather than shipping the ambiguity.
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "workspace is dirty; About would read <sha>-dirty" >&2
            git status --porcelain >&2
            exit 1
          fi
          echo "clean at $(git rev-parse --short=12 HEAD)"
      - name: release
        run: cargo bundle --release --target ${{ matrix.target }}
      - name: write LSUIElement into Info.plist
        # `make bundle` does this for local builds but hard-codes the
        # no-`--target` bundle path, so CI replicates it here. LSUIElement
        # is what keeps the Dock icon from flashing on launch:
        # hide_dock_icon() only runs ~50ms in, after gpui has built
        # NSApplication and started the runloop, and the icon would sit
        # in the Dock for all of it. Must happen BEFORE codesign — editing
        # Info.plist afterwards invalidates the signature.
        run: |
          PLIST="target/${{ matrix.target }}/release/bundle/osx/${{ env.APP_NAME }}.app/Contents/Info.plist"
          /usr/libexec/PlistBuddy -c "Delete :LSUIElement" "$PLIST" >/dev/null 2>&1 || true
          /usr/libexec/PlistBuddy -c "Add :LSUIElement bool true" "$PLIST"
          echo "LSUIElement=true -> $PLIST"
      - name: sign app
        env:
          CERT_NAME: ${{ secrets.MACOS_CERTIFICATE_NAME }}
        run: |
          echo "Signing App with identity: $CERT_NAME"
          APP="target/${{ matrix.target }}/release/bundle/osx/${{ env.APP_NAME }}.app"
          # --options runtime: enable Hardened Runtime (required for
          # notarization). No entitlements file: the app needs none —
          # no JIT, no Apple Events, plain Metal rendering.
          # --timestamp contacts Apple's timestamp server, which is
          # occasionally flaky ("A timestamp was expected but was not
          # found"); retry before failing the whole release.
          for attempt in 1 2 3 4 5; do
            if codesign --force --deep --verbose --options runtime --timestamp \
              --sign "$CERT_NAME" \
              "$APP"; then
              echo "codesign succeeded on attempt $attempt"
              break
            fi
            if [ "$attempt" -eq 5 ]; then
              echo "codesign still failing after $attempt attempts" >&2
              exit 1
            fi
            echo "codesign attempt $attempt failed (timestamp server?); retrying in 15s…"
            sleep 15
          done
          # Fail fast if no secure timestamp was embedded (notarization needs it).
          if ! codesign -dvv "$APP" 2>&1 | grep -q "^Timestamp="; then
            echo "no secure timestamp embedded in the signature" >&2
            exit 1
          fi
      - name: create dmg
        run: |
          mkdir -p release
          DMG_NAME="release/${{ env.APP_NAME }}-${{ matrix.dmg_suffix }}.dmg"
          APP_PATH="target/${{ matrix.target }}/release/bundle/osx/${{ env.APP_NAME }}.app"
          create-dmg \
            --volname "${{ env.APP_NAME }} Installer" \
            --window-pos 200 120 \
            --window-size 800 400 \
            --icon-size 100 \
            --icon "${{ env.APP_NAME }}.app" 200 190 \
            --hide-extension "${{ env.APP_NAME }}.app" \
            --app-drop-link 600 185 \
            "$DMG_NAME" \
            "$APP_PATH"
      - name: notarize dmg
        env:
          AC_USERNAME: ${{ secrets.AC_USERNAME }}
          AC_PASSWORD: ${{ secrets.AC_PASSWORD }}
          AC_TEAM_ID: ${{ secrets.AC_TEAM_ID }}
        run: |
          DMG_NAME="release/${{ env.APP_NAME }}-${{ matrix.dmg_suffix }}.dmg"
          echo "Submitting DMG for notarization..."
          xcrun notarytool submit "$DMG_NAME" \
            --apple-id "$AC_USERNAME" \
            --password "$AC_PASSWORD" \
            --team-id "$AC_TEAM_ID" \
            --wait
      - name: wait for propagation
        run: sleep 30
      - name: staple DMG
        run: |
          DMG_NAME="release/${{ env.APP_NAME }}-${{ matrix.dmg_suffix }}.dmg"
          xcrun stapler staple "$DMG_NAME"
      - name: upload asset (tag)
        if: startsWith(github.ref, 'refs/tags/')
        # The release (draft or hand-published) is guaranteed to exist —
        # prepare_vars created it before this job started — so a plain
        # upload is race-free. --clobber makes re-runs idempotent.
        run: |
          gh release upload "$GITHUB_REF_NAME" \
            "release/${{ env.APP_NAME }}-${{ matrix.dmg_suffix }}.dmg" --clobber
      - name: upload asset (nightly)
        # main pushes only — a tag build must not overwrite the nightly
        # release with stable-build artifacts.
        if: github.ref == 'refs/heads/main'
        run: |
          gh release upload nightly \
            "release/${{ env.APP_NAME }}-${{ matrix.dmg_suffix }}.dmg" --clobber

  # Linux tarballs. No signing, no notarization, no bundle — so this job
  # is the whole story: build, tar, upload. Two architectures because
  # Omarchy runs on x86_64 while the development machine is aarch64
  # (docs/omarchy-port.md), and the source is identical across them.
  #
  # `install-linux.sh` rides inside the tarball on purpose: the script
  # installs whatever binary sits next to it, so unpacking and running it
  # never touches the network. The same script downloads a release when
  # it is run on its own.
  linux:
    needs: prepare_vars
    if: needs.prepare_vars.outputs.should_build == 'true'
    name: linux-${{ matrix.arch }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            arch: x86_64
          - os: ubuntu-24.04-arm
            arch: aarch64
    steps:
      - uses: actions/checkout@v7
      - name: system dependencies
        # The same list test.yml installs, for the same reasons:
        # xkbcommon links at build time (gpui turns the crate's default
        # features off, so there is no dlopen shim) and gpui's x11
        # backend pulls `xkbcommon-x11` and `xcb` in with it — a missing
        # one of those surfaces at the *link* step, long after everything
        # has compiled. fontconfig backs font-kit. Wayland and Vulkan are
        # dlopened at runtime, so they are not needed to build.
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            pkg-config libxkbcommon-dev libxkbcommon-x11-dev libxcb1-dev \
            libfontconfig1-dev
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}
      - name: rust cache
        uses: Swatinem/rust-cache@v2
        with:
          key: linux-build-${{ matrix.arch }}
      - name: build
        run: cargo build --release
      - name: stage the tarball
        run: |
          set -euo pipefail
          stage="${{ env.APP_NAME }}-linux-${{ matrix.arch }}"
          mkdir -p "$stage"
          install -m755 "target/release/${{ env.APP_NAME }}" "$stage/${{ env.APP_NAME }}"
          install -m644 "assets/${{ env.APP_NAME }}-icon.png" "$stage/${{ env.APP_NAME }}.png"
          install -m755 scripts/install-linux.sh "$stage/install-linux.sh"
          tar -czf "$stage.tar.gz" "$stage"
          ls -l "$stage.tar.gz"
      - name: upload asset (tag)
        # Same shape as the macOS upload: prepare_vars created the
        # release before this job started, so a plain upload is
        # race-free and --clobber makes a re-run idempotent.
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          gh release upload "$GITHUB_REF_NAME" \
            "${{ env.APP_NAME }}-linux-${{ matrix.arch }}.tar.gz" --clobber
      - name: upload asset (nightly)
        if: github.ref == 'refs/heads/main'
        run: |
          gh release upload nightly \
            "${{ env.APP_NAME }}-linux-${{ matrix.arch }}.tar.gz" --clobber

  # One DMG that fits both chips, published as plain `zstats.dmg` so it
  # reads as the default download; the arch-suffixed DMGs stay for anyone
  # who wants the smaller file. Pulls the per-arch DMGs back off the
  # release (the same what-users-download principle as checksums) and
  # `lipo`s the executables — no compilation happens here, so the two
  # slices are byte-identical to what the per-arch DMGs ship. The binary
  # changes, so the app must be re-signed and the DMG re-notarized.
  universal:
    needs: [prepare_vars, macos]
    if: needs.prepare_vars.outputs.should_build == 'true'
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v7
      - name: install tools
        run: brew install create-dmg
      - name: Import Code-Signing Certificates
        env:
          p12_certificate: ${{ secrets.MACOS_CERTIFICATE }}
          p12_password: ${{ secrets.MACOS_CERTIFICATE_PWD }}
        run: |
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
          security create-keychain -p "" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "" "$KEYCHAIN_PATH"
          CERT_PATH="$RUNNER_TEMP/certificate.p12"
          echo "$p12_certificate" | base64 --decode > "$CERT_PATH"
          security import "$CERT_PATH" -k "$KEYCHAIN_PATH" -P "$p12_password" -T /usr/bin/codesign
          rm -f "$CERT_PATH"
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "" "$KEYCHAIN_PATH"
          security list-keychains -d user -s "$KEYCHAIN_PATH"
      - name: download per-arch dmgs
        run: |
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then tag="$GITHUB_REF_NAME"; else tag=nightly; fi
          echo "RELEASE_TAG=$tag" >> "$GITHUB_ENV"
          gh release download "$tag" --pattern '${{ env.APP_NAME }}-*.dmg' --dir dmgs
          ls -l dmgs
      - name: lipo into a universal app
        run: |
          set -euo pipefail
          APP="${{ env.APP_NAME }}.app"
          BIN="$APP/Contents/MacOS/${{ env.APP_NAME }}"
          mkdir -p mnt-arm mnt-intel
          hdiutil attach "dmgs/${{ env.APP_NAME }}-aarch64.dmg" -nobrowse -readonly -mountpoint "$PWD/mnt-arm"
          hdiutil attach "dmgs/${{ env.APP_NAME }}-x86_64.dmg" -nobrowse -readonly -mountpoint "$PWD/mnt-intel"
          # The arm bundle is the template; only the executable differs
          # between the two (the assets are compiled in).
          cp -R "mnt-arm/$APP" "$APP"
          chmod -R u+w "$APP"
          lipo -create "mnt-arm/$BIN" "mnt-intel/$BIN" -output "$BIN"
          hdiutil detach mnt-arm
          hdiutil detach mnt-intel
          # A single-slice binary here would ship as a working DMG that
          # silently dropped half its audience — fail instead.
          archs="$(lipo -archs "$BIN")"
          case "$archs" in
            *arm64*x86_64*|*x86_64*arm64*) echo "universal: $archs" ;;
            *) echo "not universal: $archs" >&2; exit 1 ;;
          esac
      - name: sign app
        env:
          CERT_NAME: ${{ secrets.MACOS_CERTIFICATE_NAME }}
        run: |
          # Same retry story as the per-arch jobs: --timestamp talks to
          # Apple's occasionally flaky timestamp server.
          APP="${{ env.APP_NAME }}.app"
          for attempt in 1 2 3 4 5; do
            if codesign --force --deep --verbose --options runtime --timestamp \
              --sign "$CERT_NAME" \
              "$APP"; then
              echo "codesign succeeded on attempt $attempt"
              break
            fi
            if [ "$attempt" -eq 5 ]; then
              echo "codesign still failing after $attempt attempts" >&2
              exit 1
            fi
            echo "codesign attempt $attempt failed (timestamp server?); retrying in 15s…"
            sleep 15
          done
          if ! codesign -dvv "$APP" 2>&1 | grep -q "^Timestamp="; then
            echo "no secure timestamp embedded in the signature" >&2
            exit 1
          fi
      - name: create dmg
        run: |
          mkdir -p release
          create-dmg \
            --volname "${{ env.APP_NAME }} Installer" \
            --window-pos 200 120 \
            --window-size 800 400 \
            --icon-size 100 \
            --icon "${{ env.APP_NAME }}.app" 200 190 \
            --hide-extension "${{ env.APP_NAME }}.app" \
            --app-drop-link 600 185 \
            "release/${{ env.APP_NAME }}.dmg" \
            "${{ env.APP_NAME }}.app"
      - name: notarize dmg
        # No credential preflight here: the per-arch jobs notarized with
        # the same secrets minutes ago.
        env:
          AC_USERNAME: ${{ secrets.AC_USERNAME }}
          AC_PASSWORD: ${{ secrets.AC_PASSWORD }}
          AC_TEAM_ID: ${{ secrets.AC_TEAM_ID }}
        run: |
          xcrun notarytool submit "release/${{ env.APP_NAME }}.dmg" \
            --apple-id "$AC_USERNAME" \
            --password "$AC_PASSWORD" \
            --team-id "$AC_TEAM_ID" \
            --wait
      - name: wait for propagation
        run: sleep 30
      - name: staple DMG
        run: xcrun stapler staple "release/${{ env.APP_NAME }}.dmg"
      - name: upload asset
        run: gh release upload "$RELEASE_TAG" "release/${{ env.APP_NAME }}.dmg" --clobber

  # Aggregate every dmg's hash into a single SHA256SUMS on the tagged
  # release. Pulls the dmgs back off the release rather than passing
  # artifacts between jobs — what gets hashed is exactly what users will
  # download.
  checksums:
    needs: [macos, universal, linux]
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    env:
      # No checkout, for the gitee job's reason: this hashes release
      # assets, not source. It used to check out, and the repo's own
      # `assets/` (fonts, icons, locales) was exactly where the downloads
      # landed. `sha256sum *.dmg` never noticed; the moment the glob
      # widened to take the Linux tarballs, the repo's directories were
      # hashed and the job failed (v0.3.4) — and had they been plain
      # files instead, they would have been *published* in SHA256SUMS.
      # Without a checkout `gh` needs the repository named.
      GH_REPO: ${{ github.repository }}
    steps:
      - name: build and upload SHA256SUMS
        run: |
          set -euo pipefail
          # A directory nothing else in this job could have made.
          gh release download "$GITHUB_REF_NAME" \
            --pattern '*.dmg' --pattern '*.tar.gz' --dir downloaded
          # Everything that was downloaded and nothing else — the Linux
          # installer verifies its tarball against this file, so a
          # release asset missing from here is one nobody can check.
          (cd downloaded && sha256sum -- * | sort -k2 > ../SHA256SUMS)
          echo "--- SHA256SUMS ---"; cat SHA256SUMS
          gh release upload "$GITHUB_REF_NAME" SHA256SUMS --clobber

  # Mirror the finished release to Gitee (gitee.com/vicanso/zstats.app).
  # GitHub's release CDN is slow to unreachable from mainland China, and a
  # DMG nobody can download is not a release — so the same files, with the
  # same names, land on a mirror that is reachable there.
  #
  # Last in the chain on purpose: it copies what the GitHub release
  # actually carries (three DMGs, two Linux tarballs, SHA256SUMS) instead of building or
  # hashing anything of its own — the same what-users-download principle
  # the universal and checksums jobs follow. Nightly is deliberately not
  # mirrored: it is a rolling build, and re-uploading ~100 MB to Gitee
  # every night to serve a tag that moves is not what the mirror is for.
  gitee:
    needs: [macos, universal, checksums]
    # Two ways in: the tag build that just finished, or a mirror-only
    # dispatch. `always()` is what lets the second one through — on a
    # dispatch the three build jobs are skipped, and a skipped `needs`
    # would otherwise take this job with it.
    if: >-
      always() && (
        (startsWith(github.ref, 'refs/tags/') && needs.checksums.result == 'success')
        || inputs.mirror_tag != ''
      )
    runs-on: ubuntu-latest
    # Uploads from a GitHub runner to Gitee run at ~11 KB/s per file
    # (measured on v0.3.3: 8.5 MB in 13m52s, 17.3 MB in 24m15s), so a
    # release of ~63 MB is an hour and a half sequential. Three hours
    # covers a slow day with retries; past that something is wrong
    # rather than slow.
    timeout-minutes: 180
    env:
      GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
      # owner/repo on Gitee. The dot is part of the repo path there.
      GITEE_REPO: vicanso/zstats.app
      # Empty on a tag build; the release to mirror on a manual run.
      MIRROR_TAG: ${{ inputs.mirror_tag }}
      # `gh` reads the repository from the checkout's git remote, and this
      # job deliberately has no checkout: it copies release assets, not
      # source. Without this every `gh` call dies on "not a git
      # repository" — which is exactly how the first mirrored release
      # failed.
      GH_REPO: ${{ github.repository }}
    steps:
      - name: mirror release assets to Gitee
        run: |
          set -euo pipefail
          tag="${MIRROR_TAG:-$GITHUB_REF_NAME}"

          if [ -z "${GITEE_TOKEN:-}" ]; then
            echo "GITEE_TOKEN is not set — cannot mirror $tag to $GITEE_REPO" >&2
            exit 1
          fi

          # A draft is not a release yet. The tag-push path leaves one
          # (publishing through the GitHub UI is the other path, and that
          # one is already public by the time this runs), and pushing a
          # draft's binaries to a public Gitee release would publish the
          # build before GitHub does. Re-run this job after publishing.
          if [ "$(gh release view "$tag" --json isDraft -q .isDraft)" = "true" ]; then
            echo "$tag is still a draft on GitHub; nothing to mirror yet"
            exit 0
          fi

          gh release download "$tag" --dir assets
          ls -l assets

          # Gitee takes the token as a query parameter (`access_token`);
          # Actions masks it in the log either way. Every JSON call goes
          # through here so a 4xx prints what Gitee said — a silent 403
          # would otherwise look exactly like a finished mirror.
          #
          # Bounded, because the link to Gitee stalls: on v0.3.5 three
          # of these calls took five minutes and an upload hung sixteen
          # before the kernel gave up on it. Retried only when the call
          # is idempotent — a POST whose response was lost may already
          # have created what a retry would create a second time.
          api() {
            local method="$1" path="$2"; shift 2
            local body status
            local retry=()
            case "$method" in
              GET | DELETE) retry=(--retry 3 --retry-delay 10 --retry-all-errors) ;;
            esac
            body="$(mktemp)"
            if ! status="$(curl -sS ${retry[@]+"${retry[@]}"} --connect-timeout 30 --max-time 180 \
              -X "$method" -o "$body" -w '%{http_code}' \
              --url-query "access_token=$GITEE_TOKEN" \
              "https://gitee.com/api/v5$path" "$@")"; then
              echo "gitee $method $path -> no answer" >&2
              rm -f "$body"
              return 1
            fi
            if [ "$status" -ge 400 ]; then
              echo "gitee $method $path -> $status" >&2
              cat "$body" >&2
              echo >&2
              rm -f "$body"
              return 1
            fi
            cat "$body"
            rm -f "$body"
          }

          # "Not there yet" is the ordinary case — the release does not
          # exist on the mirror until we make it — so this one lookup
          # reads the status itself instead of going through `api`.
          #
          # **Gitee does not 404 for a release that is not there.** It
          # answers `200` with a body of literally `null` (measured
          # 2026-09-19 against this very repo), and `jq -r .id` on that
          # prints the *string* `null`. That string became the release
          # id, the script reported "reusing Gitee release null", and
          # every later call went to `/releases/null/…` and came back 404
          # with Gitee's HTML error page — a failure that reads like a
          # missing repository, two steps away from its cause. So the
          # body decides, not the status, and the id is checked for being
          # a number before it is ever pasted into a path.
          rel="$(mktemp)"
          status="$(curl -sS --retry 3 --retry-delay 10 --retry-all-errors \
            --connect-timeout 30 --max-time 180 -o "$rel" -w '%{http_code}' \
            --url-query "access_token=$GITEE_TOKEN" \
            "https://gitee.com/api/v5/repos/$GITEE_REPO/releases/tags/$tag")"
          case "$status" in
            200) release_id="$(jq -r '.id? // empty' "$rel" 2>/dev/null || true)" ;;
            404) release_id="" ;;
            *) echo "gitee GET releases/tags/$tag -> $status" >&2; cat "$rel" >&2; exit 1 ;;
          esac
          # Anything that is not a plain number is "no release": null, an
          # empty body, an HTML page jq could not read.
          case "$release_id" in
            "" | *[!0-9]*) release_id="" ;;
          esac

          # The GitHub release's notes, so both pages say the same thing.
          # `body` is required on create and on update, and must not be
          # empty, hence the fallback for a release whose notes are blank.
          notes="$(gh release view "$tag" --json body -q .body)"
          if [ -z "$notes" ]; then
            notes="$tag — https://github.com/$GITHUB_REPOSITORY/releases/tag/$tag"
          fi

          # The release is a prerelease for as long as its attachments
          # are incomplete, and is published only once every file is
          # there at its exact size. The app's updater asks Gitee for
          # `releases/latest` and then spells each download URL: v0.3.5's
          # half mirror (SHA256SUMS and nothing else) was offered as an
          # update to everyone who cannot reach GitHub, and every
          # download 404'd. Whether Gitee's `latest` actually skips a
          # prerelease is not documented — its own description is "the
          # most recently updated release" — so the step below measures
          # it and says so in the log. Either answer leaves this no worse
          # than publishing up front.
          set_prerelease() {
            jq -nc --arg t "$tag" --arg n "$notes" --argjson p "$1" \
              '{tag_name: $t, name: $t, body: $n, prerelease: $p}' \
            | api PATCH "/repos/$GITEE_REPO/releases/$release_id" \
                -H 'Content-Type: application/json' -d @- \
            | jq -r '.prerelease'
          }

          if [ -z "$release_id" ]; then
            # `target_commitish` is required by the API. Read the mirror's
            # own default branch rather than assuming main or master: on
            # Gitee that field is whatever the repo was created with, and
            # a wrong branch name fails the create with a 404 that reads
            # like a missing repo.
            branch="$(api GET "/repos/$GITEE_REPO" | jq -r .default_branch)"
            release_id="$(
              jq -nc --arg t "$tag" --arg n "$notes" --arg b "$branch" \
                '{tag_name: $t, name: $t, body: $n, prerelease: true, target_commitish: $b}' \
              | api POST "/repos/$GITEE_REPO/releases" \
                  -H 'Content-Type: application/json' -d @- \
              | jq -r '.id? // empty'
            )"
            # Fail here rather than let an unusable id reach the upload
            # URLs: that is exactly how the `null` above got four calls
            # away from the thing that produced it.
            case "$release_id" in
              "" | *[!0-9]*)
                echo "gitee accepted the create but returned no usable release id" >&2
                exit 1
                ;;
            esac
            echo "created Gitee release $release_id for $tag on $branch as a prerelease"
            hidden="true"
          else
            # A re-run over a release that is already public: hide it for
            # the length of the upload too. Best effort — failing to hide
            # is no reason not to finish the mirror.
            hidden=""
            if hidden="$(set_prerelease true)" && [ "$hidden" = "true" ]; then
              echo "reusing Gitee release $release_id for $tag, marked prerelease while uploading"
            else
              echo "reusing Gitee release $release_id for $tag (could not mark it prerelease: ${hidden:-no answer})" >&2
            fi
          fi
          # The measurement the prerelease step depends on; only meaningful
          # while the release really is one.
          if [ "$hidden" = "true" ] \
            && latest="$(api GET "/repos/$GITEE_REPO/releases/latest" | jq -r '.tag_name // "none"')"; then
            echo "Gitee releases/latest while $tag is a prerelease: $latest"
          fi

          # Every copy of one file name, removed and confirmed gone. Gitee
          # has no --clobber: an attachment of the same name would sit
          # beside the old one and the download page would offer two
          # files with one name. The confirm is what makes it safe to run
          # again after a failed attempt — a DELETE whose response was
          # lost is retried into a 404, and the listing is the only
          # answer that means anything.
          clear_attachment() {
            local name="$1" list old
            list="$(api GET "/repos/$GITEE_REPO/releases/$release_id/attach_files?per_page=100")" || return 1
            for old in $(printf '%s' "$list" | jq -r --arg n "$name" '.[] | select(.name == $n) | .id'); do
              if api DELETE "/repos/$GITEE_REPO/releases/$release_id/attach_files/$old" >/dev/null; then
                echo "removed previous $name ($old)"
              fi
            done
            list="$(api GET "/repos/$GITEE_REPO/releases/$release_id/attach_files?per_page=100")" || return 1
            printf '%s' "$list" | jq -e --arg n "$name" 'all(.[]; .name != $n)' >/dev/null
          }

          # One file, up to three attempts. No `--max-time`: at 11 KB/s a
          # 17 MB file legitimately takes twenty-five minutes. The stall
          # guard is what replaces it — under 256 B/s for three minutes
          # is a dead connection, not a slow one (the v0.3.5 upload sat
          # at zero for sixteen minutes before the kernel's own timeout),
          # and the bar is low enough that five uploads sharing one
          # throttled link still clear it. Each attempt clears the name
          # first, because the attempt before it may have landed a copy
          # whose response never arrived.
          upload_one() {
            local file="$1" name attempt status body
            name="$(basename "$file")"
            body="$(mktemp)"
            for attempt in 1 2 3; do
              if [ "$attempt" -gt 1 ]; then
                sleep $((attempt * 20))
              fi
              if ! clear_attachment "$name"; then
                echo "$name: attempt $attempt could not clear earlier copies" >&2
                continue
              fi
              status=""
              if status="$(curl -sS --connect-timeout 30 --speed-limit 256 --speed-time 180 \
                -X POST -o "$body" -w '%{http_code}' \
                --url-query "access_token=$GITEE_TOKEN" \
                "https://gitee.com/api/v5/repos/$GITEE_REPO/releases/$release_id/attach_files" \
                -F "file=@$file")" && [ "$status" -lt 400 ]; then
                echo "uploaded $name (attempt $attempt)"
                rm -f "$body"
                return 0
              fi
              echo "$name: attempt $attempt failed (http ${status:-none})" >&2
              cat "$body" >&2 || true
              echo >&2
            done
            rm -f "$body"
            return 1
          }

          # All files at once. Each upload has sat at the same ~11 KB/s
          # on its own, which reads like a per-connection limit; if it is
          # per address instead, five in parallel take as long as five in
          # a row and nothing is lost.
          pids=()
          for file in assets/*; do
            upload_one "$file" &
            pids+=("$!")
          done
          failed=0
          for pid in "${pids[@]}"; do
            wait "$pid" || failed=1
          done

          # Say what the mirror ends up carrying, and check it against the
          # files themselves — name and byte count, exactly one of each. A
          # partial mirror that reports success is a download link that
          # 404s for the people it exists for, and a truncated one is a
          # checksum failure for them instead. A surplus copy (an attempt
          # that landed after the next one had cleared the name) is
          # removed rather than failed: its bytes are the right ones.
          uploaded="$(api GET "/repos/$GITEE_REPO/releases/$release_id/attach_files?per_page=100")"
          echo "--- gitee attachments ---"
          printf '%s' "$uploaded" | jq -r '.[] | "\(.name)\t\(.size)\t\(.browser_download_url)"'
          missing=0
          for file in assets/*; do
            name="$(basename "$file")"
            size="$(wc -c < "$file" | tr -d ' ')"
            ids="$(printf '%s' "$uploaded" | jq -r --arg n "$name" --argjson s "$size" \
              '.[] | select(.name == $n and .size == $s) | .id')"
            if [ -z "$ids" ]; then
              echo "missing on Gitee: $name ($size bytes)" >&2
              missing=1
              continue
            fi
            for extra in $(printf '%s\n' "$ids" | tail -n +2); do
              if api DELETE "/repos/$GITEE_REPO/releases/$release_id/attach_files/$extra" >/dev/null; then
                echo "removed surplus copy of $name ($extra)"
              else
                echo "could not remove surplus copy of $name ($extra)" >&2
                missing=1
              fi
            done
            if printf '%s' "$uploaded" | jq -e --arg n "$name" --argjson s "$size" \
              'any(.[]; .name == $n and .size != $s)' >/dev/null; then
              echo "a copy of $name on Gitee has the wrong size" >&2
              missing=1
            fi
          done
          if [ "$failed" -ne 0 ] || [ "$missing" -ne 0 ]; then
            echo "$tag stays a prerelease on Gitee; re-run this job to finish it" >&2
            exit 1
          fi

          published=""
          if ! published="$(set_prerelease false)" || [ "$published" != "false" ]; then
            echo "every file is on Gitee but $tag could not be published (prerelease: ${published:-no answer})" >&2
            exit 1
          fi
          echo "published $tag on Gitee"
          if latest="$(api GET "/repos/$GITEE_REPO/releases/latest" | jq -r '.tag_name // "none"')"; then
            echo "Gitee releases/latest now: $latest"
          fi
